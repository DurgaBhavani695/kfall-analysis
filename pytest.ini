[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    slow: marks tests that take more than 10 seconds (deselect with -m "not slow")
    integration: marks integration tests that require a real dataset
